fastapi
uvicorn
python-multipart
aiofiles
sqlalchemy>=2.0
pymysql
alembic
python-dotenv
httpx

# AI Pipeline
# torch/torchaudio: CPU build for now (CUDA cu121 deferred — see WORK_LOG "나중에 할 일")
torch
torchaudio
# PyPI demucs 4.0.1 sdist is missing demucs/api.py; install from git which includes it (AC3 needs the Python API)
# pinned to a commit (not HEAD) for reproducible builds
demucs @ git+https://github.com/facebookresearch/demucs@e976d93ecc3865e5757426930257e200846a520a
# ACE-Step: 로컬 원곡 음악 생성 (Story 2-2). 가중치(ACE-Step-v1-3.5B, 수 GB)는
# 최초 실행 시 data/models/ace-step/ 로 자동 다운로드(git 제외). device 자동선택(CUDA 있으면 GPU).
ace-step
# SDXL 로컬 이미지 생성 (Story 3-1). 모델 stabilityai/stable-diffusion-xl-base-1.0
# (OpenRAIL++-M, 상업 허용). 가중치 fp16 variant ~7GB는 최초 실행 시 data/models/sdxl/ 로
# 자동 다운로드(git 제외). device 자동선택(CUDA면 fp16 GPU, 없으면 fp32 CPU).
# (현재 ace-step이 transitive로 끌어오지만 image provider가 직접 의존하므로 명시.)
diffusers
transformers
accelerate
safetensors
# LTX-Video 로컬 i2v 영상 생성 (Story 3-2). 모델 Lightricks/LTX-Video
# (OpenRAIL 기반, 연 매출 $10M 미만 상업 무료). 가중치는 최초 실행 시 data/models/ltx-video/
# 로 자동 다운로드(git 제외). device 자동선택(CUDA면 bf16 + enable_model_cpu_offload로 12GB 적재,
# 없으면 fp32 CPU). 해상도/프레임은 12GB 친화 보수 기본값(512×288/49f).
# imageio-ffmpeg: diffusers export_to_video의 권장 백엔드(프레임→MP4 인코딩).
imageio-ffmpeg
# sentencepiece: LTX-Video의 T5 텍스트 인코더 토크나이저(T5Tokenizer)가 요구.
sentencepiece
# FFmpeg 최종 합성 (Story 3-3): 음악+영상+커버 → output.mp4. 별도 Python 패키지 없음 —
# stdlib subprocess로 시스템 ffmpeg를 호출한다(이미 런타임 필수 의존성: Demucs 디코딩/LTX export.
# winget Gyan.FFmpeg가 PATH에 설치됨). 합성 단계는 PATH의 ffmpeg/ffprobe에 의존.
